import React from 'react';
import './App.css';

const AdminScreen: React.FC = () => (
  <div className="container">
    <h1>Admin Settings</h1>
    <p>Admin APIs are disabled in this environment.</p>
  </div>
);

export default AdminScreen;
