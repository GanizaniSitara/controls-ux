import React from 'react';

const ConfigurationPanel: React.FC = () => {
  return (
    <div className="config-section">
      <h2 className="config-title">Configuration Panel</h2>
      <p className="text-muted">Control comparison rules and threshold configuration coming soon...</p>
    </div>
  );
};

export default ConfigurationPanel;